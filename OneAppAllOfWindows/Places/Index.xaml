<Page
    x:Name="pageRoot"
    x:Class="OneAppAllOfWindows.Places.Index"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneAppAllOfWindows.Places"
    xmlns:common="using:OneAppAllOfWindows.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center">
            <Image x:Name="pickedPhoto" Width="300" Height="300"/>
            <Button Click="Button_Click" HorizontalAlignment="Center" Width="150" Height="80">Pick A Photo</Button>
            <Button Click="Button_Click_1" HorizontalAlignment="Center" Width="120" Height="70">Next Page</Button>
        </StackPanel>
        
        
    </Grid>
</Page>
